type Message = 
 record {
   content: text;
   sender: text;
 };
service : {
  addMessage: (text, text) -> ();
  getClaudeResponse: (text) -> (text);
  getConversationHistory: () -> (vec Message) query;
}
